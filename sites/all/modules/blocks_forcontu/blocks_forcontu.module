<?php
/**
* @file
* Forms Forcontu
* Forcontu - Ejemplos de formularios
*/

/**
* Implements hook_block_info().
*/
function blocks_forcontu_block_info() {
  // Bloque Nodos no publicados
  $blocks['unpublished_nodes'] = array(
    'info' => t('Unpublished nodes'),
    'cache' => DRUPAL_NO_CACHE,
  );
  return $blocks;
}